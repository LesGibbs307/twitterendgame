import React, { Component } from 'react';

export class Hero extends Component {
    render(){
      return (
        <section className="Hero"></section>
      );
    }
  }

  export default Hero;